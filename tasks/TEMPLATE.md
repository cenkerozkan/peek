# NNN — <short imperative title>

## Goal

One or two sentences. What exists after this task that did not exist before.

## Context

Only what is needed for *this* task. Restate the relevant conventions inline — especially
any that fail silently — rather than pointing at CLAUDE.md and hoping.

## Files

You may create or modify only these:

- `path/to/file.py` — new
- `path/to/other.py` — modify: <what changes>

## Specification

The concrete work. Signatures, return shapes, column types, endpoint paths, exact names.
No room left for interpretation.

## Constraints

Hard rules for this task. Things that are wrong even if the code runs.

## Acceptance check

The exact command to run and what its output must show.

```bash
<command>
```

## Out of scope

What NOT to build here, so the agent does not helpfully expand the task.

---

## Outcome

<!-- Filled in by the coding agent before moving this file to tasks/done/ -->

**Status:**

**Files changed:**

**Acceptance check:**

**Deviations:**

**Problems:**
